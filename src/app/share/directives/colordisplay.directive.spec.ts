import { ColordisplayDirective } from './colordisplay.directive';

describe('ColordisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ColordisplayDirective();
    expect(directive).toBeTruthy();
  });
});
